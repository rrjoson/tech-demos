# tech-demos

Sticky monorepo for weekday X-bookmark tech demos.

- One app per pick under `apps/<slug>/`
- Bun as runtime / package manager / scripts
- Do **not** create a new GitHub repo per demo
- Cloud agents: plan with `skills/project-planning/`, then open **one** PR with screenshot **and** video validation artifacts

Tracking of proposed/built bookmarks lives in `tracking/seen-bookmarks.json`.
